{
  "name": "caseload-render",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated headless-Chromium render service for the Website Design & Conversion Check. Deploys as its own Vercel project with zero application secrets -- see docs/BUILD_PLAN_render_isolation_v1.md in the caseload-select repo for why this exists.",
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "tsc": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@sparticuz/chromium": "^149.0.0",
    "playwright-core": "^1.61.1",
    "undici": "^6.27.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "playwright": "^1.61.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
