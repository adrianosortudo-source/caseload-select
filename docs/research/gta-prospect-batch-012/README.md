# GTA public-roster prospect Batch 012

This is a deterministic, read-only public-evidence source queue observed on **2026-09-08**. It contains 32 records across west/north and east/outer GTA. Every record remains `accepted=false`, `import_ready=false`, and `automatic_merge=false`.

The generator reconciles against the complete repository baseline: 20 reviewed fixtures, 103 offline accepted-ledger projections, and 5,902 preserved legacy rows (15 review signals). The live ledger is `offline_pending`; a static clear result is not import approval.

Published office addresses retain suite/unit identity. Leadership names/titles and public emails are stored only when explicitly visible on the cited first-party page; unknowns remain unknown. No outreach, forms, chats, login, CRM write, import, merge, or deployment occurred.

Regenerate with `npm run prospects:generate-batch-012` and verify with `npm run prospects:validate-batch-012`.
